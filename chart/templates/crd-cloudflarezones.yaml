{{- if .Values.crds.install }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
{{- if .Values.crds.keep }}
    helm.sh/resource-policy: keep
{{- end }}
  name: cloudflarezones.cloudflare.io
spec:
  group: cloudflare.io
  names:
    kind: CloudflareZone
    listKind: CloudflareZoneList
    plural: cloudflarezones
    singular: cloudflarezone
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Domain
      type: string
    - jsonPath: .status.zoneID
      name: Zone ID
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v2alpha1
    schema:
      openAPIV3Schema:
        description: CloudflareZone is the Schema for the cloudflarezones API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudflareZoneSpec defines the desired state of a Cloudflare
              Zone.
            properties:
              cloudflare:
                description: |-
                  Cloudflare overrides the operator-level default credential (sourced
                  from the operator's CLOUDFLARE_API_TOKEN/CLOUDFLARE_ACCOUNT_ID env,
                  chart-set from a Secret). Per Foundation §5 the token and accountID
                  are inherited or overridden as a unit; CEL on this CRD must reject
                  setting only one. Omitted entirely → the operator-level env default
                  applies.
                properties:
                  accountID:
                    description: |-
                      AccountID is the Cloudflare account ID this credential scopes to.
                      Exactly one of accountID or accountIDSecretRef must be set.
                    minLength: 1
                    type: string
                  accountIDSecretRef:
                    description: |-
                      AccountIDSecretRef resolves the Cloudflare account ID from a Secret
                      instead of the inline accountID (exactly one of the two must be set).
                      NOTE: SecretReference.Key defaults to "token"; set key: accountID
                      explicitly (the account ID is typically a distinct key in the same
                      Secret as the API token).
                    properties:
                      key:
                        default: token
                        description: Key inside the Secret holding the Cloudflare
                          API token. Defaults to "token".
                        type: string
                      name:
                        description: Name of the Secret.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace of the Secret. Defaults to the referencing
                          CR's namespace.
                        type: string
                    required:
                    - name
                    type: object
                  tokenSecretRef:
                    description: TokenSecretRef points at the Secret carrying the
                      Cloudflare API token.
                    properties:
                      key:
                        default: token
                        description: Key inside the Secret holding the Cloudflare
                          API token. Defaults to "token".
                        type: string
                      name:
                        description: Name of the Secret.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace of the Secret. Defaults to the referencing
                          CR's namespace.
                        type: string
                    required:
                    - name
                    type: object
                  txtRegistryKeySecretRef:
                    description: |-
                      TxtRegistryKeySecretRef references a Secret holding an AES-256 key
                      (exactly 32 bytes, under the SecretReference.Key entry, default "key").
                      When set, the DNSRecord reconciler encrypts TXT companion-registry
                      payloads with AES-256-GCM (wire format v1:<base64-nonce>:<base64-ct>);
                      when unset, companions are written as plaintext JSON. The read side
                      auto-detects either form. See the TXT-registry design for the full
                      contract (companion naming, ownership verification, observe mode).
                    properties:
                      key:
                        default: token
                        description: Key inside the Secret holding the Cloudflare
                          API token. Defaults to "token".
                        type: string
                      name:
                        description: Name of the Secret.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace of the Secret. Defaults to the referencing
                          CR's namespace.
                        type: string
                    required:
                    - name
                    type: object
                required:
                - tokenSecretRef
                type: object
                x-kubernetes-validations:
                - message: exactly one of accountID or accountIDSecretRef must be
                    set
                  rule: '(has(self.accountID) && self.accountID.size() > 0) ? !has(self.accountIDSecretRef)
                    : has(self.accountIDSecretRef)'
              deletionPolicy:
                default: Retain
                description: |-
                  DeletionPolicy controls what happens when the CR is deleted.
                  "Retain" (default) leaves the zone in Cloudflare; "Delete" removes it.
                enum:
                - Retain
                - Delete
                type: string
              interval:
                default: 30m
                description: Interval is the reconciliation interval.
                type: string
              name:
                description: Name is the domain name to onboard (e.g., "example.com").
                minLength: 1
                type: string
              paused:
                description: Paused indicates whether the zone is paused (not serving
                  traffic through Cloudflare).
                type: boolean
              type:
                default: full
                description: |-
                  Type is the zone type. "full" means Cloudflare is authoritative DNS;
                  "partial" is CNAME setup; "secondary" mirrors an upstream master.
                  Immutable after creation.
                enum:
                - full
                - partial
                - secondary
                type: string
            required:
            - deletionPolicy
            - name
            - type
            type: object
          status:
            description: CloudflareZoneStatus defines the observed state of a CloudflareZone.
            properties:
              activatedOn:
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncedAt:
                format: date-time
                type: string
              nameServers:
                items:
                  type: string
                type: array
              observedGeneration:
                format: int64
                type: integer
              originalNameServers:
                items:
                  type: string
                type: array
              originalRegistrar:
                type: string
              phase:
                default: Pending
                description: Phase is a coarse summary derived from the Ready condition
                  (Foundation §8).
                enum:
                - Ready
                - Reconciling
                - Error
                - Pending
                type: string
              status:
                description: Status is the zone status in Cloudflare (initializing,
                  pending, active, moved).
                type: string
              zoneID:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
