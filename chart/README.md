# cloudflare-operator

![Version: 0.1.0](https://img.shields.io/badge/Version-0.1.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: 0.1.0](https://img.shields.io/badge/AppVersion-0.1.0-informational?style=flat-square)

Cloudflare operator — meta-operator + bundled controllers

This page is a **values reference** — it documents every helm value the
chart accepts. For the operator's behavior, CRD shapes, and installation
walkthrough, see the [top-level README](../README.md). For the CRD field
reference, see [`docs/crd-reference.md`](../docs/crd-reference.md).

## Requirements

| Repository | Name | Version |
|------------|------|---------|
| https://bjw-s-labs.github.io/helm-charts/ | common | 5.0.1 |

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| controllers.tunnel.connector.image | object | `{}` | Cloudflared connector image override for tunnels this operator auto-creates. Empty `{}` = use the operator's Renovate-tracked compile-time pin. Per-axis: set only `repository` to use a mirror / ECR pull-through cache (inherits the pinned tag) — the recommended Docker Hub rate-limit mitigation. Manually-declared CloudflareTunnel CRs are unaffected (use `spec.connector.image`). |
| controllers.tunnel.connector.resources | object | `{}` | Cloudflared connector resources seeded into every CloudflareTunnel this operator auto-creates from a Gateway annotation (standard k8s `ResourceRequirements`). Empty `{}` = no requests/limits. Manually-declared CloudflareTunnel CRs are unaffected. |
| controllers.tunnel.enabled | bool | `false` | Enable the tunnel controller bundle (manages CloudflareTunnel + source controllers for Service / Gateway / HTTPRoute / TLSRoute). |
| controllers.tunnel.logLevel | string | `"info"` | Log level for the tunnel controller. One of `debug` / `info` / `warn` / `error`. |
| controllers.tunnel.replicas | int | `1` | Replica count for the tunnel controller Deployment. |
| controllers.zone.enabled | bool | `true` | Enable the zone controller bundle (manages CloudflareZone, CloudflareZoneConfig, CloudflareRuleset, CloudflareDNSRecord). |
| controllers.zone.logLevel | string | `"info"` | Log level for the zone controller. One of `debug` / `info` / `warn` / `error`. |
| controllers.zone.replicas | int | `1` | Replica count for the zone controller Deployment. |
| crds.install | bool | `true` | Whether to render the 5 bundle CRDs as chart templates at all. |
| crds.keep | bool | `true` | Stamp `helm.sh/resource-policy: keep` on every CRD so they survive `helm uninstall` (data-safety default). |
| credentials.accountIDKey | string | `"accountID"` | Secret key under which the Cloudflare account ID is stored. |
| credentials.existingSecret | string | `""` | Name of the pre-existing Secret carrying the Cloudflare API token + account ID. Empty disables operator-level defaults; per-CR overrides remain available. |
| credentials.tokenKey | string | `"token"` | Secret key under which the Cloudflare API token is stored. |
| image.pullPolicy | string | `"IfNotPresent"` | Container image pull policy. |
| image.repository | string | `"ghcr.io/jacaudi/cloudflare-operator"` | Container image repository for the meta-operator. |
| image.tag | string | `""` | Container image tag. Empty → use `.Chart.AppVersion`. |
| leaderElection.enabled | bool | `true` | Controller-manager leader election (controller-runtime Lease-based HA). Required when `replicas > 1`. |
| namespace | string | `""` | Override-only / informational. bjw-s common renders the ServiceAccount and Deployment in `.Release.Namespace`, so that is the single source of truth for the operator's namespace. Leave this empty (the intended path) and install into the target namespace via `helm --namespace` / Flux HelmRelease `targetNamespace`. If set non-empty it MUST equal the release namespace — a divergent value fails the install fast (templates/common.yaml guard). |
| observability.metrics.serviceMonitor.enabled | bool | `false` | Render a Prometheus-Operator `ServiceMonitor` scraping the operator's `/metrics` endpoint. Requires Prometheus Operator's `monitoring.coreos.com/v1` CRDs to be installed in the cluster. |
| replicas | int | `1` | Meta-operator Deployment replica count. `>1` requires `leaderElection.enabled` (the chart wires `--leader-election`) so only one replica actively reconciles; the others are warm standby (HA). |
| resources | object | `{"limits":{"cpu":"500m","memory":"512Mi"},"requests":{"cpu":"100m","memory":"128Mi"}}` | Resource requests + limits for the meta-operator container. |

---

*This file is generated by `helm-docs` from `chart/values.yaml` and
`chart/README.md.gotmpl`. Do not edit by hand — run `make generate`.*
