---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: cloudflarezoneconfigs.cloudflare.io
spec:
  group: cloudflare.io
  names:
    kind: CloudflareZoneConfig
    listKind: CloudflareZoneConfigList
    plural: cloudflarezoneconfigs
    singular: cloudflarezoneconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.zoneID
      name: Zone ID
      type: string
    - jsonPath: .status.appliedSettings
      name: Settings
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CloudflareZoneConfig is the Schema for the cloudflarezoneconfigs
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of CloudflareZoneConfig
            properties:
              botManagement:
                description: BotManagement defines bot management settings for the
                  zone.
                properties:
                  enableJS:
                    description: EnableJS enables JavaScript detections.
                    type: boolean
                  fightMode:
                    description: FightMode enables bot fight mode.
                    type: boolean
                type: object
              interval:
                default: 30m
                description: Interval is the reconciliation interval.
                type: string
              network:
                description: Network defines network settings for the zone.
                properties:
                  ipGeolocation:
                    description: IPGeolocation enables IP geolocation.
                    enum:
                    - "on"
                    - "off"
                    type: string
                  ipv6:
                    description: IPv6 enables IPv6 support.
                    enum:
                    - "on"
                    - "off"
                    type: string
                  opportunisticOnion:
                    description: OpportunisticOnion enables onion routing.
                    enum:
                    - "on"
                    - "off"
                    type: string
                  pseudoIPv4:
                    description: PseudoIPv4 controls Pseudo IPv4 behavior.
                    enum:
                    - "off"
                    - add_header
                    - overwrite_header
                    type: string
                  websockets:
                    description: WebSockets enables WebSocket support.
                    enum:
                    - "on"
                    - "off"
                    type: string
                type: object
              performance:
                description: Performance defines performance settings for the zone.
                properties:
                  brotli:
                    description: Brotli enables brotli compression.
                    enum:
                    - "on"
                    - "off"
                    type: string
                  browserCacheTTL:
                    description: BrowserCacheTTL is the browser cache TTL in seconds.
                      0 means respect existing headers.
                    minimum: 0
                    type: integer
                  cacheLevel:
                    description: CacheLevel controls the cache level.
                    enum:
                    - aggressive
                    - basic
                    - simplified
                    type: string
                  earlyHints:
                    description: EarlyHints enables early hints.
                    enum:
                    - "on"
                    - "off"
                    type: string
                  http2:
                    description: HTTP2 enables HTTP/2.
                    enum:
                    - "on"
                    - "off"
                    type: string
                  http3:
                    description: HTTP3 enables HTTP/3.
                    enum:
                    - "on"
                    - "off"
                    type: string
                  minify:
                    description: Minify controls minification settings.
                    properties:
                      css:
                        description: CSS enables CSS minification.
                        enum:
                        - "on"
                        - "off"
                        type: string
                      html:
                        description: HTML enables HTML minification.
                        enum:
                        - "on"
                        - "off"
                        type: string
                      js:
                        description: JS enables JavaScript minification.
                        enum:
                        - "on"
                        - "off"
                        type: string
                    type: object
                  polish:
                    description: Polish controls image optimization.
                    enum:
                    - "off"
                    - lossless
                    - lossy
                    type: string
                type: object
              secretRef:
                description: SecretRef references a Secret containing Cloudflare API
                  credentials.
                properties:
                  name:
                    description: Name of the Secret.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              security:
                description: Security defines security settings for the zone.
                properties:
                  browserCheck:
                    description: BrowserCheck enables browser integrity check.
                    enum:
                    - "on"
                    - "off"
                    type: string
                  challengeTTL:
                    description: ChallengeTTL is the challenge TTL in seconds.
                    enum:
                    - 300
                    - 900
                    - 1800
                    - 2700
                    - 3600
                    - 7200
                    - 10800
                    - 14400
                    - 28800
                    - 57600
                    - 86400
                    type: integer
                  emailObfuscation:
                    description: EmailObfuscation enables email obfuscation.
                    enum:
                    - "on"
                    - "off"
                    type: string
                  securityLevel:
                    description: SecurityLevel controls the security level.
                    enum:
                    - essentially_off
                    - low
                    - medium
                    - high
                    - under_attack
                    type: string
                type: object
              ssl:
                description: SSL defines SSL/TLS settings for the zone.
                properties:
                  alwaysUseHTTPS:
                    description: AlwaysUseHTTPS redirects all HTTP requests to HTTPS.
                    enum:
                    - "on"
                    - "off"
                    type: string
                  automaticHTTPSRewrites:
                    description: AutomaticHTTPSRewrites rewrites HTTP URLs to HTTPS
                      in page content.
                    enum:
                    - "on"
                    - "off"
                    type: string
                  minTLSVersion:
                    description: MinTLSVersion is the minimum TLS version.
                    enum:
                    - "1.0"
                    - "1.1"
                    - "1.2"
                    - "1.3"
                    type: string
                  mode:
                    description: Mode is the SSL mode.
                    enum:
                    - "off"
                    - flexible
                    - full
                    - strict
                    type: string
                  opportunisticEncryption:
                    description: OpportunisticEncryption enables opportunistic encryption.
                    enum:
                    - "on"
                    - "off"
                    type: string
                  tls13:
                    description: TLS13 controls TLS 1.3 setting.
                    enum:
                    - "on"
                    - "off"
                    - zrt
                    type: string
                type: object
              zoneID:
                description: ZoneID is the Cloudflare Zone ID.
                minLength: 1
                type: string
            required:
            - secretRef
            - zoneID
            type: object
          status:
            description: status defines the observed state of CloudflareZoneConfig
            properties:
              appliedSettings:
                description: AppliedSettings is the count of settings applied.
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncedAt:
                description: LastSyncedAt is the last time the zone config was successfully
                  synced.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recently observed generation
                  of the CR.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
