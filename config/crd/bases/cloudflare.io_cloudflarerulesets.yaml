---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: cloudflarerulesets.cloudflare.io
spec:
  group: cloudflare.io
  names:
    kind: CloudflareRuleset
    listKind: CloudflareRulesetList
    plural: cloudflarerulesets
    singular: cloudflareruleset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Ruleset Name
      type: string
    - jsonPath: .spec.phase
      name: Phase
      type: string
    - jsonPath: .status.ruleCount
      name: Rules
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CloudflareRuleset is the Schema for the cloudflarerulesets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of CloudflareRuleset
            properties:
              description:
                description: Description is an informative description of the ruleset.
                type: string
              interval:
                default: 30m
                description: Interval is the reconciliation interval.
                type: string
              name:
                description: Name is the human-readable name for the ruleset.
                minLength: 1
                type: string
              phase:
                description: Phase is the phase of the ruleset.
                enum:
                - http_request_firewall_custom
                - http_request_firewall_managed
                - http_request_late_transform
                - http_request_redirect
                - http_request_transform
                - http_response_headers_transform
                - http_response_firewall_managed
                - http_config_settings
                - http_custom_errors
                - http_ratelimit
                - http_request_cache_settings
                - http_request_origin
                - http_request_dynamic_redirect
                - http_response_compression
                type: string
              rules:
                description: Rules is the list of rules in the ruleset.
                items:
                  description: RulesetRuleSpec defines a single rule within a Cloudflare
                    Ruleset.
                  properties:
                    action:
                      description: Action is the action to perform when the rule matches.
                      enum:
                      - block
                      - challenge
                      - js_challenge
                      - managed_challenge
                      - log
                      - skip
                      - execute
                      - redirect
                      - rewrite
                      - route
                      - score
                      - serve_error
                      - set_cache_settings
                      - set_config
                      - compress_response
                      - force_connection_close
                      type: string
                    actionParameters:
                      description: ActionParameters contains action-specific parameters
                        as free-form JSON.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    description:
                      description: Description is an informative description of the
                        rule.
                      type: string
                    enabled:
                      default: true
                      description: Enabled indicates whether the rule is active.
                      type: boolean
                    expression:
                      description: Expression is the filter expression for the rule.
                      minLength: 1
                      type: string
                  required:
                  - action
                  - expression
                  type: object
                minItems: 1
                type: array
              secretRef:
                description: SecretRef references a Secret containing Cloudflare API
                  credentials.
                properties:
                  name:
                    description: Name of the Secret.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              zoneID:
                description: |-
                  ZoneID is the Cloudflare Zone ID.
                  Mutually exclusive with ZoneRef.
                type: string
              zoneRef:
                description: |-
                  ZoneRef references a CloudflareZone resource in the same namespace.
                  The controller resolves the zone ID from the referenced resource's status.
                  Mutually exclusive with ZoneID.
                properties:
                  name:
                    description: Name of the CloudflareZone resource.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
            required:
            - name
            - phase
            - rules
            - secretRef
            type: object
          status:
            description: status defines the observed state of CloudflareRuleset
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncedAt:
                description: LastSyncedAt is the last time the ruleset was successfully
                  synced.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recently observed generation
                  of the CR.
                format: int64
                type: integer
              ruleCount:
                description: RuleCount is the number of rules in the ruleset.
                type: integer
              rulesetID:
                description: RulesetID is the Cloudflare Ruleset ID.
                type: string
            type: object
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: one of zoneID or zoneRef is required
          rule: has(self.spec.zoneID) || has(self.spec.zoneRef)
        - message: zoneID and zoneRef are mutually exclusive
          rule: '!(has(self.spec.zoneID) && has(self.spec.zoneRef))'
    served: true
    storage: true
    subresources:
      status: {}
